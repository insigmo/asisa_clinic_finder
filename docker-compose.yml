services:
  bot:
    build:
      context: .
      dockerfile: build/Dockerfile
      target: runtime
    container_name: asisa_clinic_finder_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./db/:/app/db
    command: >
      sh -c "/app/build/entrypoint.sh"
